# Security Policy

Report a Labeloo security concern privately to `security@wiplash.ai`. Do not
include real addresses, credentials, or other sensitive personal data in the
initial report.

Labeloo intentionally has no backend, remote API, analytics client, or host
permissions. Selected text is accepted only through the browser's context-menu
event and stored locally until the editor consumes it.
